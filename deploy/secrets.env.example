### Example environment file for DashFleet (copy to /var/www/dashfleet/.env and secure permissions)
# Replace values with your real secrets and settings
FLEET_TOKEN=change_this_fleet_token
ACTION_TOKEN=change_this_action_token
FLEET_TTL_SECONDS=600
WEBHOOK_URL=
WEBHOOK_MIN_SECONDS=300
# Optional: host/port overrides for gunicorn (if your service uses them)
HOST=127.0.0.1
PORT=8000

### Notes:
# - Store this file outside version control and set restrictive permissions (chmod 640).
# - On systemd, you can point to this file with EnvironmentFile=/var/www/dashfleet/.env
# Example secrets file for DashFleet
# Copy to /var/www/dashfleet/.env and set secure permissions

FLEET_TOKEN=d2f6f9a8-3c7e-4c1f-9b0f-123456789abc
ACTION_TOKEN=change_this_if_needed
HOST=127.0.0.1
PORT=8000
WEBHOOK_URL=
FLEET_TTL_SECONDS=600
